optimizer : {
  type: AdamW,
  kwargs: {
  lr : 0.00008, #0.0001
  weight_decay : 0.001 #0.0005
}}

#scheduler: {
#  type: LambdaLR,
#  kwargs: {
#  decay_step: 42, #21
#  lr_decay: 0.9,
#  lowest_decay: 0.02  # min lr = lowest_decay * lr
#}}

scheduler: {
  type: CosLR,
  kwargs: {
    t_max: 160,               # This corresponds to t_initial in the scheduler
    min_lr: 0.000001,            # This corresponds to lr_min in the scheduler
    initial_epochs: 4,       # This corresponds to warmup_t in the scheduler  #10
    warmup_lr_init: 0.00001,
    t_in_epochs: True
}}

bnmscheduler: {
  type: Lambda,
  kwargs: {
  decay_step: 21,
  bn_decay: 0.5,
  bn_momentum: 0.9,
  lowest_decay: 0.01
}}

dataset : {
  train : { _base_: cfgs/dataset_configs/ShapeNet_Car_Seq.yaml,  #cfgs/dataset_configs/PCN.yaml
            others: {subset: 'train'}},
  val : { _base_: cfgs/dataset_configs/ShapeNet_Car_Seq.yaml, 
            others: {subset: 'test'}},
  test : { _base_: cfgs/dataset_configs/ShapeNet_Car_Seq.yaml, 
            others: {subset: 'test'}}}
            
model : {
    NAME: AdaPoinTr, 
    num_query: 512, #512
    num_points: 16384,
    center_num: [128, 64],  #[512, 256], [128, 64]
    num_point_clouds: 1,  #
    global_feature_dim: 1024, 
    encoder_type: graph,
    decoder_type: fc,  #fc
    encoder_config: {
      embed_dim: 384,
      depth: 6,
      num_heads: 6,
      k: 8,
      n_group: 2,
      mlp_ratio: 2.,
      block_style_list: ['attn-graph', 'attn', 'attn', 'attn', 'attn', 'attn'], 
      combine_style: 'concat',
    },
    decoder_config: {
      embed_dim: 384,
      depth: 8,
      num_heads: 6,
      k: 8,
      n_group: 2,
      mlp_ratio: 2.,
      self_attn_block_style_list: ['attn-graph', 'attn', 'attn', 'attn', 'attn', 'attn', 'attn', 'attn'], 
      self_attn_combine_style: 'concat',
      cross_attn_block_style_list: ['attn-graph', 'attn', 'attn', 'attn', 'attn', 'attn', 'attn', 'attn'], 
      cross_attn_combine_style: 'concat',
    }
}
  
total_bs : 16 #16  #4
step_per_update : 4 #1 #2  #4
max_epoch : 160 #600

patience: 20

consider_metric: CDL1